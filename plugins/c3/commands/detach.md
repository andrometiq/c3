---
description: Release all held routes, or one named held route.
argument-hint: "[web | telegram | <topic-name>]"
allowed-tools: ["mcp__plugin_c3_c3__detach"]
---

User typed: $ARGUMENTS

With no argument, call `mcp__plugin_c3_c3__detach` with no arguments to release ALL routes. With an argument, call it with the user's argument as `target` to release only that held route. Display the response verbatim. If it is an error, display it verbatim.
